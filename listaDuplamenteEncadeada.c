#include <stdio.h>
#include <stdlib.h>
/*lista duplamente encadeada com funções para adicionar elementos, 
buscar na lista, imprimir na ordem normal ou inversa, remover elementos
da lista, concatenar 2 listas, copiar uma lista encadeada para um ponteiro*/
Struct dados{
	int value;
	struct dados* next;
	struct dados* ant;
};
typedef struct dados dados;
int main(){
	return 0;
}
